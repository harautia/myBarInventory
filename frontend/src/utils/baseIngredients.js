// Shared dough, common to every pie recipe: butter, wheat flour, baking
// powder and quark. Used to split the "Base" chapter out on the Recipe page
// and to split its stock between recipes on the Plan production page.
const BASE_INGREDIENT_NAMES = ['butter', 'wheat flour', 'baking powder', 'quark']

export default BASE_INGREDIENT_NAMES
